import {
  formatInterval
} from 'timetracker/helpers/format-interval';

module('FormatIntervalHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = formatInterval(42);
  ok(result);
});
